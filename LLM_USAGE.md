Briefly describe how you used LLMs and list the prompts you used.

I used LLM to implement certain functions, such as asking how to make thickness, color, or trapezoidal lines using Chatgpt LLm, or asking for color codes used in example images, and I wrote prompts in Korean, so I listed the prompts in Korean like this.

해당 예시 이미지에서 사용하는 "Class 숫자" 및 여타의 색깔을 그대로 사용하려고 해. 글자색이든, 사각형의 색이든. 이때 예시이미지에서 사용하고 있는 각 색의 색깔 코드를 말해줘. 

Projection view에서 빈공간을 클릭 시 기존 선택된 것이 해제되게 하려고 해. 이때 구현해야 하는 기능을 알려줘.

점들을 Projection할때, 그 좌표의 범위를 계산하려고 해. 이때 사용되는 공식이 어떤 거야? 즉 스케일링 함수를 말하는거야.

각 뷰의 적절한 사이즈를 말해줘. svg상 적절한 width와 height의 크기 값만.

개발자 도구로 확인해보니. 화면을 벗어나서 점이 찍히는 오류가 발생하고 있어. 이를 해결해기 위해, 범위값을 어떤 값으로 수정해야 하는지 알려줘.

회색 선이 뾰족하지 않고, 네모낳게 나오게 하는 방법을 알려줘.

회색선의 사다리꼴에 대해서, 대각선 성분에 너무 치우쳐지지않는, 공식을 알려줘

projection.json에서 인덱스로 json상 정의된 값들을 가져오는 것이 가능해?

jsx 파일에서 array 만드는 방법을 알려줘. 길이는 10으로. 

소수점 값을 0 ~ 9의 인덱스로 변환시키는 공식을 알려줘

선택한 아이템들을 해제하는 방법을 알려줘. 

textAnchor 은 무슨 기능을 하는거야?

stroke의 옵션을 조절할수있는 다른 변수가 존재해? 

circle, rect 등에서 key를 사용하면 어떻게 각각을 관리할 수 있어?

item을 볼 때, 해당 아이템의 아이디도 함께 보는 이유가 뭐야? 

클릭한 이벤트가 부모 이벤트까지 올라가게 하는 방법을 알려줘

이미지와 텍스트 정보를 가로로 나란히 배치하기 위한 스타일 값을 알려줘. 즉, div style={{}} 에서 써야할 값을 말이야 

style 함수의 각 파라미터 정보를 알려줘.

div 내에서 이미지 출력하기 위해 무엇을 써야해?

이미지를 확대했을 때 흐릿하게 보정하지 말고, 픽셀 느낌을 유지하려고 해. 이때 사용할 수 있는 옵션이 있어?

어레이를 만들고, 화면상에 0 ~ 1.0 까지 표현하기 위해 무엇을 써야해?

여기서 라벨이 선택되지않았을 때 필요한 조건이 더 있어? 현재는 if (selectedItem && (selectedItem.true_label !== i || (selectedPredictedLabel !== null && selectedItem.predicted_label !== selectedPredictedLabel)))  이렇게 만들었어. 

polyline에 대해서 자세히 설명해줘 

이미지와 polyline이 겹치지 않게 해주는 방법을 알려줘. 

<<<<<<< HEAD
flatmap 함수에 대해 자세히 설명해줘 
=======
flatmap 함수에 대해 자세히 설명해줘 
>>>>>>> my-work
